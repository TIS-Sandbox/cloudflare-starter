{
  "name": "tis-cloudflare-starter",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Cloudflare Pages starter for TIS Sandbox projects.",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-08-05",
    "build": "node scripts/build.js",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^4.74.0"
  }
}
